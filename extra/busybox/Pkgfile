# Pkgfile for busybox

name=busybox
version=1.36.1
release=1
source=(https://www.$name.net/downloads/$name-$version.tar.bz2)

pkg_build() {
	make defconfig
	make
	install -Dm0755 busybox $PKG/bin/busybox
}
