# Pkgfile for xpkg

name=xpkg
version=230831
release=1
source=(xpkg xpkg.conf finddeps revdep xchroot)

pkg_build() {
	for s in xpkg finddeps revdep xchroot; do
		install -Dm755 $s $PKG/usr/bin/$s
	done
	install -Dm644 xpkg.conf $PKG/etc/xpkg.conf
}
