# Pkgfile for libarchive

name=libarchive
version=3.7.0
release=1
source=(https://github.com/$name/$name/releases/download/v$version/$name-$version.tar.xz)
build_opt="--without-xml2"
bootstrap_opt=$build_opt
